name: Site CI

on:
  push:
    branches: [main]
    paths:
      - "RaceControlSite/**"
      - ".github/workflows/site-ci.yml"
  pull_request:
    paths:
      - "RaceControlSite/**"
      - ".github/workflows/site-ci.yml"
  workflow_dispatch:

concurrency:
  group: site-ci-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    working-directory: RaceControlSite

jobs:
  build:
    name: lint & build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: RaceControlSite/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build
